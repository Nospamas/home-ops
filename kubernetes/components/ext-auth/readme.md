# Authentik External Auth

- Create Proxy Provider in Authentik (authentik now comes with embedded outputs but may need integration to be selected)
- Create `ReferenceGrant` in `authentication/authentik/references` per namespace